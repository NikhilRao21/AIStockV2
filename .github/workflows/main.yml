name: Remote SSH Command
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Execute remote SSH commands using password
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: cp ~/AIStockV2/trading_system.db ~/Documents/ && rm -rf ~/AIStockV2/ && git clone https://github.com/NikhilRao21/AIStockV2.git && cp ~/Documents/.env ~/AIStockV2/ && cp ~/Documents/trading_system.db ~/AIStockV2/
